# ============================================================================
# CCTV Camera Analyzer - Environment Configuration Template
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the actual .env file to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# OpenAI API Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here


# ----------------------------------------------------------------------------
# Google Cloud Storage (GCS) Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# Set to 'true' to enable GCS image storage, 'false' for local storage
# When enabled, uses LAZY LOADING (downloads images on-demand, not upfront)
# Perfect for 1000+ images - minimal disk space, fast startup!
USE_GCS_STORAGE=false

# Your Google Cloud Project ID
# Example: my-project-123456
GCS_PROJECT_ID=your_project_id_here

# Path to your GCS service account key JSON file
# Example: gcs-key.json or /path/to/service-account-key.json
GOOGLE_APPLICATION_CREDENTIALS=gcs-key.json

# Your GCS bucket name where images are stored
# Example: my-cctv-images-bucket
GCS_BUCKET_NAME=your_bucket_name_here


# ----------------------------------------------------------------------------
# Google Maps API Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# Required for map visualization features
# Get your API key from: https://console.cloud.google.com/google/maps-apis
# Make sure to enable "Maps JavaScript API" in Google Cloud Console
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here


# ----------------------------------------------------------------------------
# Analyzer Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# Directory containing camera images (or GCS prefix)
# For local: test, camera_images, etc.
# For GCS: prefix in bucket like "test", "2024/images", etc.
IMAGES_DIR=test

# Excel file with camera metadata (IP, location, coordinates, etc.)
# Must contain columns: CAMERA IP, Location Name, LATITUDE, LONGITUDE, etc.
EXCEL_FILE=13data.xlsx

# Number of concurrent workers for image processing
# Recommended: 5-10 (adjust based on API rate limits)
MAX_WORKERS=5


# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. OPENAI SETUP (Required):
#    - Sign up at https://platform.openai.com/
#    - Create API key at https://platform.openai.com/api-keys
#    - Copy key and paste above
#    - Ensure you have GPT-4o Vision access
#
# 2. GCS SETUP (If using Google Cloud Storage):
#    a) Create GCS bucket:
#       - Go to https://console.cloud.google.com/storage
#       - Create new bucket (select appropriate region)
#       - Upload your images to the bucket
#    
#    b) Create service account:
#       - Go to https://console.cloud.google.com/iam-admin/serviceaccounts
#       - Create service account with "Storage Object Viewer" role
#       - Create key (JSON format) and download
#       - Save as gcs-key.json in project root
#    
#    c) Configure .env:
#       - Set USE_GCS_STORAGE=true
#       - Set GCS_PROJECT_ID to your project ID
#       - Set GOOGLE_APPLICATION_CREDENTIALS=gcs-key.json
#       - Set GCS_BUCKET_NAME to your bucket name
#
# 3. GOOGLE MAPS SETUP (Optional, for map visualization):
#    - Go to https://console.cloud.google.com/
#    - Enable "Maps JavaScript API"
#    - Create API key (with appropriate restrictions)
#    - Copy key and paste above
#
# 4. LOCAL SETUP (If NOT using GCS):
#    - Set USE_GCS_STORAGE=false
#    - Place images in IMAGES_DIR (default: test/)
#    - Ensure Excel file is in project root
#
# ============================================================================

